#include "common.h"

int capture_preinit(cap_backend_config_t *backend_config, cap_imagedata_callback_t imagedata_cb){ return 1; }
int capture_init(){ return 1; }
int capture_start(){ return 1; }
int capture_terminate(){ return 1; }
int capture_cleanup(){ return 1; }